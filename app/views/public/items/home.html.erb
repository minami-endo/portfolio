
<div class="main_container">
    <div class="row">
        <div class="col-lg-3"></div>
      	<div class="col-lg-6">
			<h1 class="company_name">sunny milk</h1>
        </div>
		<div class="col-lg-3"></div>
	</div>
  <div class="row">
    <div class="flex">
      <div class="home_items">
        <% @items.each do |item| %>
            <%= link_to public_item_path(item) do %>
              <%= attachment_image_tag item, :image, size:'150x100' %>
            <% end %>
        <% end %>
        <%= paginate @items %>
        </div>
    </div>
  </div>

<% if public_user_signed_in? %>
  <p>月間レシピランキング一位の方には豪華商品をプレゼントします</p>
<% else %>
  <p>会員登録すると月間レシピランキングが見られるようになります</p>
<% end %>

<h2>会社概要</h2>

<h2>工場見学</h2>
</div>
